- if cake_plan_user_signed_in?
   = link_to('Logout', destroy_cake_plan_user_session_path, :method => :delete, :class => "btn btn-primary btn-sm")

- else
   = link_to('Login', new_cake_plan_user_session_path, :class => "btn btn-primary btn-sm" )
