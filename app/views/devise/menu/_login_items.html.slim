- if cake_plan_user_signed_in?
   = link_to('Déconnexion', destroy_cake_plan_user_session_path, :method => :delete, :class => "btn btn-primary btn-default", :style => "margin : 20px 0 0 10px;")

- else
   = link_to('Se connecter', new_cake_plan_user_session_path, :class => "btn btn-primary btn-default", :style => "margin : 20px 0 0 10px;")
